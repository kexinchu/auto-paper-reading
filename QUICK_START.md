# 快速启动指南

本指南帮助你在5分钟内开始使用自动论文阅读工具。

## 前置要求

- Python 3.8+
- pip

## 步骤1: 安装依赖（1分钟）

```bash
cd auto-paper-reading
pip install -r requirements.txt
```

## 步骤2: 获取API密钥（2分钟）

选择以下任一免费/低价API提供商：

### 选项A: DeepSeek（推荐，价格最低）
1. 访问 https://platform.deepseek.com/
2. 注册并登录
3. 在API Keys页面创建新密钥
4. 复制密钥（以`sk-`开头）

### 选项B: 硅基流动（有免费额度）
1. 访问 https://siliconflow.cn/
2. 注册并登录
3. 获取API密钥

### 选项C: OpenAI（质量最高）
1. 访问 https://platform.openai.com/
2. 注册并充值
3. 创建API密钥

## 步骤3: 配置API（1分钟）

编辑 `config.yaml`，找到 `llm` 部分：

```yaml
llm:
  api_base: "https://api.deepseek.com"  # 根据你选择的提供商修改
  api_key: "sk-xxxxxxxxxxxxx"  # 粘贴你的API密钥
  model: "deepseek-chat"  # 根据提供商修改模型名称
```

**或者** 使用环境变量（更安全）：

```bash
export OPENAI_API_KEY="sk-xxxxxxxxxxxxx"
export OPENAI_API_BASE="https://api.deepseek.com"
export OPENAI_MODEL="deepseek-chat"
```

## 步骤4: 配置邮箱（1分钟）

编辑 `config.yaml`，找到 `email` 部分：

```yaml
email:
  smtp_server: "smtp.163.com"  # 163邮箱
  smtp_port: 465
  sender_email: "your-email@163.com"  # 发件邮箱
  sender_password: "AUTHORIZATION_CODE"  # 授权码（不是登录密码！）
  recipient_email: "your-email@gmail.com"  # 收件邮箱
```

### 获取163邮箱授权码：
1. 登录 mail.163.com
2. 设置 → POP3/SMTP/IMAP
3. 开启"POP3/SMTP服务"
4. 获取授权码

### 其他邮箱配置：

**QQ邮箱：**
```yaml
smtp_server: "smtp.qq.com"
smtp_port: 465
```

**Gmail：**
```yaml
smtp_server: "smtp.gmail.com"
smtp_port: 587
use_tls: true
```

## 步骤5: 测试运行（立即）

```bash
# 测试所有组件
python main.py --test

# 如果测试通过，立即执行一次完整任务
python main.py --run-now
```

## 步骤6: 自定义主题（推荐）

编辑 `topics.yaml` 添加你感兴趣的研究主题。本工具采用**关键词+LLM双重筛选**机制：

```yaml
topics:
  - name: "大语言模型优化"
    description: "关注大语言模型的训练和推理优化，包括KV-Cache优化、量化、剪枝、蒸馏等技术"
    keywords:  # 关键词列表（用于快速预筛选）
      - "LLM"
      - "large language model"
      - "transformer"
      - "KV cache"
      - "quantization"
      - "inference optimization"
      - "model compression"
    # required_keywords:  # 可选：必须包含的关键词
    #   - "optimization"
  
  - name: "计算机视觉"
    description: "关注图像识别、目标检测、图像分割、视频理解等领域"
    keywords:
      - "computer vision"
      - "image recognition"
      - "object detection"
      - "image segmentation"
      - "CNN"
      - "vision transformer"

# 筛选配置
filtering:
  keyword_match_threshold: 0.1  # 至少匹配10%关键词才进入LLM评估
  min_relevance_score: 6  # 相关性至少6分
  min_quality_score: 6  # 质量至少6分
  enable_quality_assessment: true  # 启用质量评估
```

**配置建议**：
- **关键词**：列出领域的核心术语，越全面越好（10-20个）
- **主题描述**：详细说明关注的具体方向
- **阈值调整**：根据需要调整评分阈值（更高=更严格）

## 步骤7: 启动定时任务（可选）

```bash
# 启动定时任务（根据config.yaml中的时间自动运行）
python main.py
```

默认每天22:30（纽约时间）自动运行。

## Docker部署（可选）

如果你想使用Docker：

```bash
# 1. 创建.env文件
cat > .env << EOF
OPENAI_API_KEY=sk-xxxxxxxxxxxxx
OPENAI_API_BASE=https://api.deepseek.com
OPENAI_MODEL=deepseek-chat
EOF

# 2. 启动服务
docker-compose up -d

# 3. 查看日志
docker-compose logs -f
```

## 常见问题

### Q: API调用失败？
A: 检查：
1. API密钥是否正确
2. 账户余额是否充足
3. 网络连接是否正常
4. API基础URL是否正确

### Q: 没有收到邮件？
A: 检查：
1. SMTP设置是否正确
2. 是否使用了授权码（不是登录密码）
3. 垃圾邮件文件夹
4. 查看日志文件 `paper_reader.log`

### Q: 论文筛选效果不好？
A: 
1. **优化关键词**：在 `topics.yaml` 中添加更多相关关键词（10-20个）
2. **详细描述**：提供更具体的主题描述
3. **调整阈值**：
   - 降低 `keyword_match_threshold`（如0.05）包含更多论文
   - 提高 `min_relevance_score`（如7-8）只保留高相关论文
   - 提高 `min_quality_score`（如7-8）只保留高质量论文
4. **更好的模型**：使用 gpt-4o-mini 效果更佳
5. **关闭质量评估**：如只关心相关性，设置 `enable_quality_assessment: false`

### Q: 成本问题？
A: 
- **新方案**（关键词预筛选）：约￥0.26/天，￥8/月
- **节省65%成本**：相比旧方案（全部LLM评估）
- **成本构成**：
  - 关键词筛选：免费
  - 相关性评估：￥0.15/天
  - 质量评估：￥0.045/天
  - 内容提取：￥0.06/天
- **进一步优化**：关闭质量评估可节省20%，使用免费API（硅基流动）

## 进阶配置

### 修改运行时间

编辑 `config.yaml`：

```yaml
schedule:
  time: "08:00"  # 每天早上8点
  timezone: "Asia/Shanghai"  # 使用中国时区
```

### 修改arXiv分类

编辑 `config.yaml`：

```yaml
arxiv:
  categories:
    - "cs.AI"  # 人工智能
    - "cs.LG"  # 机器学习
    - "cs.CV"  # 计算机视觉
    - "cs.CL"  # 自然语言处理
```

完整分类列表：https://arxiv.org/category_taxonomy

### 调整筛选阈值

编辑 `config.yaml`：

```yaml
filtering:
  min_score: 7  # 提高阈值，只保留高度相关的论文（0-10分）
```

## 获取帮助

- 查看完整文档：[README.md](README.md)
- 提交问题：GitHub Issues
- 查看日志：`paper_reader.log`

## 下一步

恭喜！你已经成功配置了自动论文阅读工具。现在：

1. 等待定时任务运行，或使用 `--run-now` 立即执行
2. 检查你的邮箱，查看论文摘要
3. 根据需要调整 `topics.yaml` 和 `config.yaml`
4. 享受自动化的论文追踪！

Happy Reading! 📚

